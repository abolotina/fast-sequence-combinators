#lang info

;; ========================================
;; pkg info

(define pkg-name "fast-sequence-lib")
(define pkg-desc "implementation (no documentation) part of \"fast-sequence\"")
(define version "0.0")
(define collection "fast-sequence")
(define license
  '(Apache-2.0 OR MIT))

(define deps
  '(["base" #:version "7.4"]))

;; ========================================
;; collect info

(define name "fast-sequence")
